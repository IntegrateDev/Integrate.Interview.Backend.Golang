basePath: /
definitions:
  server.Result:
    properties:
      errors:
        items: {}
        type: array
      result: {}
    type: object
host: localhost:8084
info:
  contact: {}
paths:
  /health:
    get:
      description: get server health
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/server.Result'
            type: array
        "500":
          description: ""
      summary: Health Check
      tags:
      - health
swagger: "2.0"
